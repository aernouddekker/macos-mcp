{
  "private": true,
  "workspaces": [
    "packages/mail",
    "packages/numbers",
    "packages/contacts",
    "packages/calendar",
    "packages/reminder",
    "packages/print",
    "packages/facetime"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "prebuild": "./scripts/check-shared-drift.sh",
    "clean": "rm -rf packages/*/dist"
  }
}
